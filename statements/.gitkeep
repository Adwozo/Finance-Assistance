# Drop bank/credit-card statements (CSV, TXT, or PDF) in this folder.
# The app scans it on demand from the Import page or the /api/import endpoint.
# Use the MCP `convert_statement` tool to normalize non-CSV files into canonical CSV first.
